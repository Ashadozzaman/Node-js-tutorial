## Controlled Component

- A Controlled Component in React means that the form element’s value is controlled by React state rather than being handled by the DOM itself.

### short:

- Controlled Component = Input controlled by React’s state.
- Uncontrolled Component = Input controlled by the DOM.

### Hooks = Functions that add “React power” to functional components.
